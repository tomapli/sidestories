After a big job, run a CodeRabbit review before finalizing when the CodeRabbit CLI is authenticated and available.

Default review scope:

- `mode`: `plain`
- `type`: `uncommitted`
- `cwd`: repository root

If CodeRabbit is not authenticated, report that clearly and continue with the normal local verification that fits the job.
